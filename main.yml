---
- hosts: localhost
  user: hishikiy
  gather_facts: no
  connection: local

  vars_files:
    - vars/main.yml

  roles:
    - homebrew
    - pip
#    - dotfiles
#    - atom-packages
    - gem
    - npm
    - gcloud

  tasks:
    - include: tasks/ansible-setup.yml
